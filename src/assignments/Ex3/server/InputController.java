package assignments.Ex3.server;

import assignments.Ex3.model.Direction;

public class InputController {
    // הכי מינימלי: תחליף בהמשך לקלט מ-StdDraw (isKeyPressed וכו')
    public Direction nextDir() {
        return Direction.STAY;
    }
}
